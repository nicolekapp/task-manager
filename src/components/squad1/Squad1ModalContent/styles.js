import { makeStyles } from "@material-ui/core/styles";

const styles = makeStyles((theme) => ({
  root: {
    height: 300,
    padding: 15,
  },
  texInput: {
    width: 500,
  },
}));

export default styles;
