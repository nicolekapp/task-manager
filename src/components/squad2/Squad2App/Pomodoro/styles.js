import { makeStyles } from "@material-ui/core/styles";

const styles = makeStyles(() => ({
    buttonContainer: {
        width: "15%",
    },
    button: {
        fontSize: 10
    }
}));

export default styles;
